export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}

export enum BillingCycle {
  MONTH = 'month',
  YEAR = 'year',
}

export enum Currency {
  USD = 'USD',
}

export enum Steps {
  PRODUCT = 'product',
  PLAN = 'plan',
}
